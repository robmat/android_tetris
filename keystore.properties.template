## This project reads its signing credentials from ../keystore.properties
## (one folder up, shared across all game projects in this workspace — gitignored, never committed).
## storeFile is resolved relative to THIS project's root, not the shared folder.
storeFile=../dev_keystore.jks
storePassword=
keyAlias=
keyPassword=
